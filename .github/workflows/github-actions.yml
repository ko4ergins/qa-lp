name: ☂ run tests

on:
  workflow_dispatch:

jobs:
   test:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v2
      - name: Docker build
        run: docker build -t img-tst .
      - name: Run tests
        run: docker run img-tst .
