name: ☂ run tests

on:
  workflow_dispatch:

jobs:
   test:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v2

      - name: Build docker image
        run: docker build -t img-tst .

      - name: Test report
        run: echo http://134.209.192.159/${{ github.run_number }}

      - name: Run tests
        run: docker run -v /home/report/${{ github.run_number }}:/app/report/ -t img-tst:latest
