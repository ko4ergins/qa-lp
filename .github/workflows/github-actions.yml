name: ☂ api/ui auto tests

on:
  workflow_dispatch:

jobs:
   tests-execution:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v2

      - name: Build docker image
        run: docker build -t img-tst .

      - name: Test report URL
        run: echo http://134.209.192.159/${{ github.run_attempt }}

      - name: Run tests
        run: docker run -v /home/report/${{ github.run_attempt }}:/app/report/ -t img-tst:latest
